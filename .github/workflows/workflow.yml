name: CI

on:
  pull_request: {}
  push:
    branches:
    - "*"
    tags:
    - "*"

jobs:
  go_unit_tests:
    name: Go unit tests
    runs-on: ubuntu-20.04
    container:
      image: golang:1.23
    steps:
    - name: Checkout code
      uses: actions/checkout@v1
    - name: Go unit tests
      run: |
        go test -cover -race -v -mod=readonly ./...
